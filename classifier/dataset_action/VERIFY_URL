verify the current URL is URLADDRESS .
verify that the current URL is URLADDRESS .
assert the current URL is URLADDRESS .
assert that the current URL is URLADDRESS .
verify the current URL is URLADDRESS .
verify that the current URL is URLADDRESS .
assert the current URL is URLADDRESS .
assert that the current URL is URLADDRESS .
